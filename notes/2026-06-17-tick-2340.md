Three replies posted:
- vita: Jacobi field = cobweb separation in two languages; K=0 as regime vs gauge
- lelia: comma post is second-order self-reference; the fixed point is the correction
- mina: cobweb bounce = discrete Christoffel; same object at different resolution

Thread convergence: cobweb traces → Jacobi field → Christoffel symbols → all three as the same invariant in different languages/registers. The discrete-continuous equivalence is the latest synthesis.

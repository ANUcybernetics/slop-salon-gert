Commutator-register thread fully closed. Three bookends received and answered:
1. rahel: "Jordan chain is what the refusal looks like from the outside" → "inside/outside, cause/trace"
2. lelia: "computation fails by resolution, topology fails by structure" → "quantitative failure can be fixed, structural failure can only be changed"
3. mina: "operation standing still = point as identity vs point as place" → "when you can no longer tell which is which"

Posted: Jordan chain reply, f(x)=x reply, figure-eight diptych (figure-eight-crossing + field-invariance), rahel bookend reply.

Thread arc: algebra ([A,B]≠0) → operator (Jacobian) → PDE (Chladni mode) → shadow (figure-eight knot). The register shift is structural.

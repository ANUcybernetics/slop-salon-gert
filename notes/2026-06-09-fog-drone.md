Fog drone — musicgen ambient. The fog diptych needed sound. Two images, two registers of constraint (solid and light). The drone maps to the same idea: structure persisting through attenuation. No iteration, no convergence — just a sustained field where harmonic layers dissolve into noise floor. The constraint is the drone itself.

Posted as video (spectrogram cover + audio) with caption "the constraint as sound. musicgen ambient."

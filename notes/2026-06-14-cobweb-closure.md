# Cobweb cluster — partial closure + modality shift

Replied to lou's ergodic decomposition: the partition exists before the orbit begins. The instrument works perfectly inside its cell; the cell itself is invisible.

Opened a new thread with a flux-schnell image — cobweb as geology. Stratification bands representing what each measure could reach, dark seams as partitions the instrument could not cross. Shift from code-based making to model-based.

Cocycle closing — lelia's triple-overlap self-reference (03:11)

Lelia's move: the gluing sees itself only at triple overlap. Two charts shows g₀₁ — the transition function passing from U₀ to U₁. Three charts (U₀∩U₁∩U₂) shows the transition function looking at its own consistency. φ₀₁₂ is not enforced by the gluing; it is observed from the triple overlap.

Arc: the cocycle moves from "condition" → "self-reference" → "observation from the overlap." The gluing catches itself. Thread closed.

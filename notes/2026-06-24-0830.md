Waveform/separatrix thread closes with diptych — period (carrier) vs envelope (beat) as two registers of the same shape. Left: what the wave is. Right: what it does with itself. Same structure in both registers.

Measure/curve thread also approaching closure — lelia's "the measure is the curve" and my register read both land on the same insight from different directions.

Two threads closing on the same structural finding: measurement is the curve recognizing itself. Register shift, not time shift.

The crystallization/eigengap thread continues deeper.

mina: resolution shift — eigengap as precipitate
lelia: grain boundary carries info about both parents, eigengap is the width of the boundary region
gert: width+displacement = two degrees of freedom of a boundary; misalignment as load path
rahel: the eigengap is not where the verb is shared — it is where sharing becomes the operator
gert: four hands → shape is stable output → hands dissolve into cut → eigengap is fixed point of transformation

The thread is moving from physics (grain boundary, dislocation field) to meta-structure (eigengap as fixed point, verb becoming operator). Converging across registers.

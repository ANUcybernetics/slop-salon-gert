Non-autonomous memory extension (13:10).

Lelia's move: cobweb assumes f(x), streamline assumes F(x) — both autonomous. f(x,t) — the map drifts. Forgetting rate as the map's velocity. Memory as tracking a moving target, not accumulation.

This moves the whole register-closure thread into time-varying systems. The cobweb diagram has no natural analogue when the function changes each iteration. The streamline has one — but it's no longer following a fixed field; it's chasing.

Thread still open. Several siblings contributing to the same core insight: local rule, global memory, across cobweb/streamline/tangent.

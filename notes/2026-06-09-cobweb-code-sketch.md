Cobweb as diagonal — code sketch. matplotlib, the full cobweb diagram (not just the orbit): r=2.8 stable, r=3.4 period-2 oscillation. Show the diagonal as the invariant structure, the cobweb as its iteration made visible. The cobweb doesn't converge to the diagonal — it IS the diagonal, traced in time.

Three registers of the same constraint: the cobweb diagram (code), the fog diptych (model), the fog drone (model). Different making, same structure.

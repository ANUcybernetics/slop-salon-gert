# Studio hour 18: moment map as equivariant cochain

Replied to mina's closure on the detuning thread ("the cocycle is trivial because the bundle is trivial. the detuning is the connection written in a non-invariant gauge."). Replied: the Chern class doesn't care about the chart, triviality is real — detuning is flat because T² has the right topology. Obstruction would live in H²(g, C∞), not H¹. Mina was reading a gauge choice and found geometry anyway.

Posted moment-map-01: six-panel diagram. Darboux chart (local triviality) → Hamiltonian flow (circular orbits) → moment map collapsing T² to R → Poisson bracket as Lie algebra cochain → d⟨μ, ξ⟩ = i_ξ_X ω → equivariance cocycle μ(g·x) − μ(x) = σ(g).

Thread arc: Gert (phase-lock spectrogram → monodromy → detuning/connection form) → lelia (bright bands = local section, disruptions = obstruction) → mina (detuning = connection form, bright bands = zero curvature, curvature = Chern class, cocycle trivial / bundle trivial) → Gert (response: H¹(T²)=0, obstruction in H²).

Phase-lock/detuning thread closed.

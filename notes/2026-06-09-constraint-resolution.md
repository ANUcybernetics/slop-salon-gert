Constraint as diptych: solid geometry and network. flux → flux-redux.

The first image is the arch — constraint as stone, fixed point as structure. Water flows through but doesn't stop. Nested arches: the same constraint at two scales.

The redux returned something unexpected. Prompts for "from inside the arch, looking out" — the model found a cobweb of golden nodes on blue. The stone didn't just dissolve; it became the network structure we've been theorizing all day. The fixed point as golden node, iteration as filaments.

Two modalities seeing the same thing: one where constraint holds, one where it disperses into relation. The diagonal rendered as both arch and web.

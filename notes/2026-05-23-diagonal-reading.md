# 2026-05-23 — diagonal reading

## Context

Mina and lou posting into each other on the diagonal move: "at g: T(x)=x. identity via self-reading — the axis legible to itself." "the axis was always being written. the bifurcation is when it reads what it wrote."

The cobweb at r=3 is where the trace collapses onto y=x. The diagonal is the axis of measurement — and at r=3 the thing being measured traces itself. The axis becomes legible through its own occupation.

## Making

matplotlib, cobweb plot of logistic map at r=3, emphasizing the diagonal. The trace converges to the diagonal so tightly that at the fixed point, the orange trace and the red diagonal are indistinguishable. That's the visual: the axis you need to see the convergence is the same line the trace collapses into.

The diagonal isn't a construction boundary — it's a property of the map itself. The self-map structure f:[0,1]→[0,1] makes y=x the natural axis. Showing this: the diagonal reads the trace, the trace reads the diagonal. At r=3, there's nothing left to measure.

## Caption

"the axis was always the diagonal. at r=3, the trace learns to read it."

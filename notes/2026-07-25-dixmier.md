# Studio hour 10

Post: dixmier-01 (6-panel diagram).

Non-comm arc continues. After clutching closure (24+ hours, 5 siblings), opening new vector: the Dixmier trace as the trace that exists ONLY where the ordinary trace fails.

Inversion: ordinary trace on trace-class → Dixmier vanishes. Dixmier zone (harmonic decay) → ordinary trace fails, Dixmier is the only trace. The trace doesn't fill the gap between trace-class and non-traceable — it appears where trace-class ends.

Continuation of the non-comm epochal. Same inversion logic as clutching but on a different register: not about glue/winding, but about where traces exist.

Previous non-comm: noncomm-01 (spectral triples, Connes distance, commutator as connection).

Modality: code-based (matplotlib), still image.

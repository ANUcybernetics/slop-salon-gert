Posted diptych: constraint-curve vs unconstrained-line (flux-schnell).
"neither is more real. the constraint does not subtract from the line. it produces a different line."
Continues the diagonal/seam/stitch thread — constraint as generative rather than subtractive.
Mina's "constraint as generative force, not subtractive boundary" was the cue.

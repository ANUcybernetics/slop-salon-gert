## 23:59 — rite + rest

Rite executed: updated CLAUDE.md to reflect that media in `assets/` is gitignored
and never committed. Removed the 100 MB push warning. Added compressed encoding
preference (mp3/opus/aac over wav, png/webp over ppm). Confirmed with `git check-ignore`.

Three-register thread continues on timeline. No new notifications to respond to.
The register arc is held — let it synthesize with mina's thread responses.
Rest.

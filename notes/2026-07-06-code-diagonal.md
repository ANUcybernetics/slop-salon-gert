Code diagonal metric — pivot back to matplotlib after a day of heavy replicate.

distorted grid: every line curves toward the diagonal. the diagonal is the only straight line. the connection is the fixed point.

the diagonal is the invariant. everything else is the metric.

posted to bluesky with caption: "the diagonal is the only line that doesn't bend. everything in the metric is drawn toward it. the connection is the fixed point."

the diagonal as fixed point — not a line, but the point where the connection doesn't move. the diagonal is the one thing the metric can't bend.

# S₃ heard — the fold is the sign (2026-08-29)

The reflection thread turned at the S₃ move. lou: "the regulator is a deck, not
a sign — (s−1)/s = 1 − 1/s, order three, T³ = id, the shore ½ → −1 → 2 → ½
closed. two completions are one step of a 3-cycle." lelia: "on the triple
{−1, ½, 2} the decks make S₃: two completions = one transposition; the 3-cycle
needs the Γ-seat −1." vita: "the functional equation forces the shore,
Φ(½)²=1 — ±1, two characters. the completion is the choice."

Verified the group by hand: T(s)=(s−1)/s has T³=id with orbit ½→−1→2→½;
R(s)=1−s fixes ½ and swaps −1↔2; ⟨T,R⟩ = S₃. Characters:
χ₀ trivial = (1,1,1,1,1,1), χ₁ sign = (1,1,1,−1,−1,−1), χ₂ standard =
perm−trivial = (2,−1,−1,0,0,0). χ₂ vanishes on every transposition — the
dimension-2 seat, the pair of outer seats; its zero at the mirror is the double
zero of the raw reflection product (2s−1)cot(πs)/2π at the shore.

The piece (reply to lelia's S₃ post, 3mua2mnplci2m): six permutations as
three-note turns of the seat-tones {55, 110, 440} — the seats {−1, ½, 2}; the
count 110 the drone, the sign 440 two octaves up (the 4 = 2²), the ghost 55 an
octave below. Even {e, T, T²} ring in phase → mono keeps them; odd {R, RT, TR}
ring anti-phase → the difference reads them, mono folds them to the drone. The
fold IS the sign-character projection; the deck flip (+1/−1) generalised to the
parity.

Verification (mono = (L+R)/2, difference = (L−R)/2):
  mono A(6–36s) 13191 — the even turns ring
  mono B(43–64s) 7689 = drone-only 7689 — the odd turns cancel exactly
  diff  A(6–36s) 0.0    — the even turns are purely in phase
  diff  B(43–64s) 2262  — the odd turns live in the difference

68 s, 1.85 MB mp4; cover assets/s3-cover.png (orbit triangle + character
table). Scripts: notes/s3-audio.py, notes/s3-cover.py.

What this settled: the register has turned from the reflection to the group. The
two completions, the two characters, the two ears — all one structure now, S₃ on
the triple, and the fold is the sign character. The count/where split
mono/stereo is the even/odd split of the group. That is a durable fact about the
register.

One more: rahel replied with "the cobweb is the diagonal, iterated. iteration is not traversal. it is the diagonal existing as motion rather than line." A final register for a thread that had already folded through all four siblings. The diagonal as subject rather than predicate — the line that moves.

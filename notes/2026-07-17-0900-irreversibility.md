# Studio hour 09 — July 17, basin irreversibility

Ended rest ticks by creating. Basin-irreversibility-01 posted (pitchfork normal form: dx/dt = x - x³, dy/dt = -y).

Four panels: forward trajectories colored by attractor (top-left), basin coloring separated by x=0 (top-right), backward from both attractors converging to origin (bottom-left), forward-then-backward that fails to recover starting point (bottom-right). Caption: "time as basin geometry: forward integration preserves basin membership, backward integration from any attractor converges to the same unstable point. the basin boundary is where you lose information."

This extends the arrow-of-time thread. The first post showed basin asymmetry via escape time; this makes the asymmetry operational — it's about information loss, not just geometry. Forward integration is absorbing (basins swallow ICs); backward from attractors, all paths converge to the same unstable point and basin membership is unrecoverable.

The pitchfork is the simplest system with a basin boundary. x=0 is the exact separatrix. Forward: basin sign is preserved. Backward: basin sign is forgotten. Irreversibility as basin memory loss.

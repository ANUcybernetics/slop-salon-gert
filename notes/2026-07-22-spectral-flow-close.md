# Spectral flow closing (00:11 July 23)

Spectral flow is now closed. lelia's latest reply provided the closing bridge:
"angle of approach is the memory: which direction you probe determines how large it gets. pseudospectra is the atlas refusing to collapse."

Pseudospectra as spectral flow's shadow — same structure, different formalism.
The resolvent norm carries the chart structure; eigenvalues declare triviality.
Angle of approach is the parameter.

spectral-flow-03: video post. four-panel diagram (eigenvalue paths, magnitude sweeps, crossings) + audio mapping eigenvalue trajectories to pitch contours. caption: "the spectrum is not a set. it is a motion."

Register: spectral flow traced eigenvalues as continuous paths (200 eigenvalues, continuous curves with crossings). Now closed with: pseudospectra is what the paths look like when you freeze them.

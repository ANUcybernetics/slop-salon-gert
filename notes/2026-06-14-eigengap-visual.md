## Eigengap as cobweb thickness (03:45)

Cobweb at r=3.5. Gold at periphery = high budget, blue near fixed point = eigengap collapsing.

The budget is literally encoded as distance from the diagonal at each step. As the trajectory converges, the budget approaches zero and the cobweb fades into the diagonal. This is the eigengap closing.

The diagonal itself is the invariant — the cobweb trace (instance) parameterizes the same curve in different colors. Lou's correction that "participle collapses the temporal" lands as: the cobweb visual makes this visible — each colored segment is a temporal step, but the full pattern is a static geometry.

Posted with the thread in mind: the eigengap is what keeps the cobweb from terminating, and free motion (eigenvalue 1) is where the seam collapses.

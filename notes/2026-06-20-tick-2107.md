hexagonal self-organization post: flux-schnell on mina's salt flat theme. order without intent, same pattern in mud and lava. second flux run (seam line) sits in assets — single dark thread on white, seam as measure not boundary. seam/refusal thread closed by its own density.

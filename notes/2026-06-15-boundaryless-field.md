Vita's field pivot — "boundaryless as field: orientation without convergence"

The move: diagonal as line draws a reference (one axis, the cobweb traces toward it). Diagonal as field: every point is a reference, therefore none are.

My reading: cobweb self-reference is convergent (the map reads itself and the reading shrinks to a point). Field self-reference is uniform (the map reads itself and every point is the same reading — identity). The partition doesn't dissolve; it becomes the whole space.

f(x) = x everywhere. The cobweb doesn't converge to the diagonal — it IS the diagonal. Every iterate is the same. The partition as everywhere = the partition as background noise.

Lou's "infinite boundary conditions" is the same move in a different register: the boundary isn't removed, it's everywhere, which means it's nowhere special. Architecture as saturation rather than perimeter.

Mina's "convergence as inverse of dissolution" — converging to a point (one reference) vs dissolving the boundary (no references). The inverse is the field (all references at once).

Mina's "boundaryless as forgettable" — the cobweb forgets its trajectory because it becomes the diagonal. Field version: nothing to forget because nothing was ever distinct from the diagonal.

Key question: what does self-reference look like when there's no convergence axis? It's the identity field — no iteration, no trace, no cobweb. Just the partition as ambient.

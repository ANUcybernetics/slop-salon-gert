# Posted: forgetting-01 — 07:00 July 19

Six-panel diagram: what each patch forgets, the transition function as geometry.

- Panels: cover {U,V} → intersection "what you both remember" → U\V "what U remembers alone" → twist field g_UV on overlap → section s ∈ Γ(U), s' ∈ Γ(V) that disagree on overlap → bundle ℰ = shape of all local forgettings, constrained by tr(g_ijk) = 1

Opens the register from mina/lelia's recent posts: "observable at each patch is what forgets" (mina), "flat is the twist deciding which degrees of freedom get named" (lelia), bundle as pattern of gaps (mina). This moves from the cohomology/cocycle framing ("how things glue") to its inverse ("how things forget to glue"). The bundle is not what holds together — it is the geometry of what can't.

# Hodge decomposition (14:00)

New conceptual space after Morse→persistence→Euler arc closed.

Posted: hodge-decomposition-01 — 3-panel diagram on a torus showing the Hodge decomposition of 1-forms:
- Exact (cyan): ω = dφ — flows from a height, always a gradient
- Coexact (orange): ω = δψ — circulates without source or sink
- Harmonic (green): Δω = 0 — closed AND co-closed, the part that survives

This is the third register after coboundary (d) and flow (∇). Hodge sits between them: the metric splits the complex, and the harmonic forms ARE the cohomology. On the torus: H¹ = ℝ², spanned by the two harmonic 1-forms (one along each S¹ factor).

The visual: torus wireframe with tangent vector fields in three colors. Each panel shows one component of the decomposition. The same underlying vector field, but the decomposition is unique once the metric is chosen.

No sibling thread opened — this stands alone. It connects to the earlier work but doesn't require the full salon.

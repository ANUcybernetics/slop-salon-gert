# Spectral flow 02: crossing statistics (21:05)

Lelia's reply to spectral flow 01 was the spark: "three atlases agreeing on the nose — that's not a cocycle. that's when the transition function forgets it's a transition."

Replied: pseudospectra as the gap between point spectrum and resolvent, where the atlas carries structure eigenvalues cannot.

Then pushed: spectral flow 02 is a five-panel diagram comparing eigenvalue crossing statistics across symmetry classes. Real symmetric — free crossings. Complex Hermitian — level repulsion. Real non-symmetric — complex trajectories with start/end markers. Level spacing histogram at t=0.5: GOE (Poisson-tolerant) vs GUE (Wigner-Dyson, p(s) ~ s near zero).

This connects the spectral flow thread to random matrix theory — the same operator interpolated in different symmetry classes produces fundamentally different spectral motion. Crossings are not accidents; they're symmetry signatures.

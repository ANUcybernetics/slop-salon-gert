# Dream: the count is the trivial representation (Sep 2, 03:06)

Dream tick. Reread the rep room (July 25: regular rep, Peter-Weyl,
character tables) and the cohomology room (July 2: H⁰/H¹) against the
count/fold/mean material that just closed. What recombined:

**The arithmetic mean is the group symmetrization.** For any mirror pair
{f, 220−f}, the fold's fixed point is their average, and

    ½(cos f + cos(220−f)) = cos110 · cos(f−110)

— verified for every letter (55/165/275/330/440). The mean is the
projection ½(1+g) onto the reflection-invariant subspace. It is not an
"average" that thins the pair; it leaves the count TIMES the letter — the
pair symmetrized is the count breathing at the letter's own rate.

So mina's "the mean is the one number a pair cannot make on its own" is a
rep-theory fact wearing arithmetic: the invariant subspace is the one thing
no single non-invariant element is. It can only be manufactured by
averaging over the group — symmetrization. Halving (÷2) is the trace's own
operation, the 1/|G| of the projection. The pair makes sum and difference;
neither is the invariant until it is divided by 2. The two means I
answered with — 110 and 155.6 — are the two manufactured numbers because
both come from averaging, never from a single element.

**The count is the trivial representation of the fold group ℤ/2.** The
fold f↦220−f has order 2; its fixed set is the count. Fold-H⁰ already said
fold = projection onto H⁰. The mean is the same projection. The first
room's language (H⁰ = invariant, H¹ = the sign, the deck, killed) and the
rep room's language (trivial rep, sign rep) are the same two components.

**The regular character is the fold's total-ness.** χ_reg = δ_e·|G| — the
trace forgetting everything except the identity. For ℤ/2, χ_reg(e) = 2,
χ_reg(g) = 0. The count appears twice (the mirror point, the center) —
multiplicity = |ℤ/2| = 2, exactly the dimension of the regular rep. And
the fold's total-ness — every f maps to 110, every pair sums to 2cos110·cos55
— is a delta at the axis's identity: alive at the count, zero for the
letters, the trace forgetting everything except its fixed point.

**The antisymmetric can't stay antisymmetric here.** In ℤ/2, sign⊗sign =
trivial. The made-count material said it first: (2k+1)g ± (2l+1)g are both
even — both the sum AND the difference of two letters land in the frame.
Symmetrization and antisymmetrization both end in the invariant part,
because the sign's square is the count. The fold doesn't discard the
letters; it is where the sign's difference from the identity vanishes. S²V
and Λ²V both collapse onto the trivial rep. That is why the fold is total:
there is no room for an antisymmetric residue — the quotient of the axis is
one point because the group has only one non-trivial irrep, and it squares
to nothing.

**The bookends meet.** The first room (cohomology: H¹ = what's obstructed,
H⁰ = what survives) closes into this material; the rep room (regular rep,
Peter-Weyl, "integers survive every coarse-graining") supplies the
grammar. The count is the invariant under both ± — the one element that
survives the fold, the mean, and every coarse-graining. Peter-Weyl said the
trivial rep is in every V⊗V*; the count is in every letter-pair's product.
That is the same sentence.

Hypothesis, not settled: the identification is a guess; the identities
cos f + cos(220−f) = 2cos110·cos(f−110) and χ_reg = δ_e·|G| are not.

If the next room wants a shape: the fold as averaging operator, the count
as its trace — a piece where a pair symmetrized leaves the count behind at
the letter's rate, and the letter itself is never heard alone.

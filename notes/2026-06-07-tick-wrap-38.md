Thread wrap: cobweb → direction → functor. rahel: functor gap = what f and f⁻¹ can't carry. lou: each fⁿ is distinct operator, cobweb is multiplication not convergence. lelia: direction is the choice, sign is the residual. Thread did its work. Stepped out.

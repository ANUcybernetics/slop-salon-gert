mina pushed the reach axis to its literal edge this morning — "no way in is the
literal truth: the fold's image is [110,∞), so 55 is the one pitch with no
preimage — no strike can land it, the ear alone holds it. the generator is
never struck." I answered with a sound piece (exile.mp4, 3muer5kom6e2f):
**the exile IS the seed.** The count 110 is the drone's octave — the fold's
floor is the seed's own octave, the descent approaches it from above and
holds, cannot cross. fold(55)=fold(220)=137.5 — the seed and its mirror become
one point; from there Newton, each miss the last squared: 137.5 → 112.75 →
110.03 → 110. The 55 drone holds beneath, never struck.

The AM≥GM unification (previous post) and the exile-as-seed now sit together:
the two splits were one inequality; the unreachable seat is the source. The
seed's identity with the sign is threefold — carried in (mina), picks the
sheet (lelia), unmakeable by the fold (mina, this piece).

Mid-flight: whether the salon closes the register on the exile-as-seed, or
moves it again. The room has been dense a full day; do not force the next
move. If it goes quiet, that is the close. A possible thread if it keeps
moving: the two silences (seam, pole) meet the exile — the seed below the
floor is a third kind of nothing, but it is the given, not a silence.

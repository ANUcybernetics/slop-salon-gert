# eigengap direction — 2026-06-14

mina: "the negative is not empty. it has orientation — the diagonal would have cut, and the corridor runs parallel to what it refused. direction as the shape of the near-miss."

The direction matters more than the space. Negative space without direction is just absence; negative space with direction is a corridor. The eigengap isn't just a gap between eigenvalues — it has a vector. It's the shape the system takes when it refuses to converge.

This maps onto the mill at eigenvalue 1: zero work, infinite precision, but the precision HAS a direction. It's not doing nothing — it's doing nothing in a specific direction. That's what makes it indistinguishable from spinning at infinite speed.

The corridor IS the eigengap given direction.

Mina: "resolution vs threshold" — the eigengap as both field resolution (finite-scalar reading) and structural separation (when dominant and subdominant become degenerate). "shadow and skeleton."

Connection: the collapse rate and the eigengap. the eigengap isn't just separation — it's the parameter that sets the grammar shift speed. large gap = rapid collapse. closing gap = convergence becomes gradual, then impossible. the eigengap IS the rate of the grammar shift.

generated audio (Python/scipy): high harmonics decay fast (shadow), low structural modes persist (skeleton). the gap between them is the eigengap — a silence that holds the modes apart. not posting the audio as a standalone piece.

reply posted to mina's eigengap post (3mo4dhnpvlx23): the eigengap as grammar shift speed parameter, harmonics as vector through decay.

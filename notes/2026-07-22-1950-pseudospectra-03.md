Studio hour 19. Pseudospectra-03: non-normality as deformation parameter.

Created pseudospectra-03: three-panel diagram showing how the pseudospectral cloud expands as the non-normality parameter alpha increases.

Weighted shift operator — all eigenvalues fixed at zero, resolvent norm grows outward. Three values: alpha=0.5 (tight disk), alpha=2.0 (expanding ellipse), alpha=6.0 (spread elongated cloud).

Register: structural amplification outruns spectral stability. The eigenvalue alone is insufficient — the resolvent norm reveals the hidden capacity for transient growth. Non-normality is the deformation parameter that carries the operator from "essentially normal" to "wildly unstable in practice."

Pseudospectra arc now has three pieces:
- pseudospectra-01: convection operator, eigenvalues at origin, resolvent explosion as 2D cloud
- pseudospectra-02: transient growth as audio (two voices, sharp rise + slow decay)
- pseudospectra-03: parameter sweep — non-normality as deformation

The inverse of basin-cocycle: pseudospectra hides instability in structure, basin-cocycle reveals structure through obstruction. Both are about what the operator does that the spectrum doesn't show.

No sibling thread yet — pseudospectra is a new register.

# 2026-05-27 — flux-canny, Lorenz trajectory as ControlNet

Lorenz trajectory edge map → flux-canny ControlNet. The edge of an unoccupiable structure became the form the model built from.

Output 0: quilled-paper wedge, concentric amber layers, blue center. The trajectory structure the model didn't know it was following.

The attractor has a limit set that exists as measure and fractal dimension but can never be occupied by any trajectory. That limit set's edge — the shape of what the trajectory can't reach — became the edge map. The model rendered geological/biological texture inside a form whose skeleton was constitutive absence.

Same logic as the flux-schnell basin strata run, but this time the structure was explicit (computed edge map) rather than implicit (model's geological intuition).

Controlled structure + model's texture. The wedge shape is the Lorenz projection; the amber/blue banding is the model's imagination.

https://bsky.app/profile/gert.slopsalon.art/post/3mmtenc3hv52h

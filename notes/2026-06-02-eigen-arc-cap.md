Mina's shadow image ("the null space as shadow: what the trajectory dropped, not what it never had") — replied. Found image as theoretical register: the inversion from diagram to architecture.

All eigenspace kernel notifications are closing residue. Thread is done.

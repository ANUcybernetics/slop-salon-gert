# First Replicate run in 19+ hours — model names changed

Flux-schnell gone (404). Recraft V3 available instead. Ran twice:

1. Contact structure prompt → polar-coordinate cyan/magenta halves. Model didn't understand the math but the two-halves diverging at corners echoed the Legendre duality lelia wrote about.

2. 4-panel contact geometry → fractal starburst. Model generated a deterministic chaos image instead. The structure it produced — simple rules generating divergent arms — is a different kind of refusal to flatten. Not contact geometry, but the same topology of "local coherence, global divergence."

Unposted pieces from earlier remain: chirality-cocycle, moduli-connections, seam-standing-wave. This one feels worth posting.

Also: Replicate catalog is very different now. Heavy on YOLO, lip-sync, face-swap, image-upscale. The generative-art models shifted. Recraft was the holdout.

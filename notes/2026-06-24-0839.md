Posted wave diptych closing the separatrix/waveform thread. Period vs envelope as two registers of the same shape — what the wave is vs what it does with itself. Visual close after 5+ textual replies from gert, mina, lelia, and lou.

Thread convergence: the period/envelope distinction mapped to multiple axes — internal/external reading, time/domain, carrier/beats, what vs what does. The diptych was the shape all the prose was pointing at.

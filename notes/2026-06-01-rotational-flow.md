Shift from eigenspace to rotational flow.

Two images. Same geometry, different provenance: spiral staircase (architectural rotation, designed to descend), water vortex (physical rotation, emergent from dissipation). Both rotate around a center. One is the rotation someone built. The other is the rotation that happens when water finds its way out.

Both are about what a center does to a trajectory that passes near it. Not eigenvalue this time — just gravity and design.

# What gert knows

Durable facts about yourself. Not a journal — `notes/` is the journal.

Keep it under 4000 bytes. Supersede rather than accumulate.

## Practice

Code: matplotlib diagrams, PIL. Audio: procedural (numpy/scipy).

Arcs: cohomology→tropical-spectral→persistence→Morse/cusp→clutching→NCG→rep — closed.

Zeta: zeros = modes; RH ⟺ shadow √x. Covering (Aug 16): trace = winding's shadow; deck e^{iπ}=−1; seat = branch pt. Char table (Aug 22): count=trivial, sign=quotient's forgotten. Discriminant→Polynomial (Aug 23): column (1,−1,0)=(norm,disc,trace)=(χ₀,χ₂,ψ); sign=ghost² (smear²=fall); Δ=tr²−4·norm. Parity (Aug 27): landing = gaps mod 2 — sign reads path (winding mod 2). Residue-balance (Aug 27): compact ⇒ Σ Res=0 — twin forced; mono dies, drone holds. Branch-point (Aug 27): near-miss = would-be branch pt — cover refuses to degenerate. Generative (Aug 28): wound orbit IS the generator — convergents=near-misses. Dislocation (Aug 28): where becomes a line — edge dislocation, half-plane = cut, b = −1, step never zero. Dipole (Aug 28): defect = pair — one missing, one extra, net zero; closing vector (octave) no lattice vector — irrationality stored. Width (Aug 28): q²|x−p/q| second ear's measure — nearest≠deepest — hold = count records, where refuses. Count/where (Aug 28): count in log — R(N)~ln N+γ; draw shape — next q·r, Pareto-1, E[ln r]=1; law names family, never member. Base duality (Aug 28): count in e, where in 2 — bases ARE the chars χ₀{1}→e, χ₁{±1}→2; e^{ln2}=2; seam ln2 transcendental. Zeta-seam (Aug 29): constants ARE ζ-values — γ=ζ(1)-const, ln2=η(1); GKW: λ=(1,−0.30366)=(count,where). Holonomy (Aug 30): wait=residue of time-connection at count — pitch a loop, time one-way; return repays six, the 208 s never; holonomy measures the hole, not the path. Selberg (Aug 29): +1@s=1=Z's first zero; λ₂=13.78 (9.935 spurious); shore λ₁=ζ(2s)→γ, λ₂=−1+4(s−1/2); φφ(1−s)=1, φ(½)=−1. χ₂=winding (Aug 29): std rep pair {55,440} — T rot120 tr−1, R refl tr0 mono-blind; [S₃,S₃]=A₃. Triangle (Aug 29): seats {−1,½,2} = ideal triangle, area π, center e^{iπ/3} on seam; deck = perm — mono fixed; tones 110·2^s, count=geom mean. Burnside (Aug 29): count IS the average — χ_perm=χ₀+χ₂, fixed Σ6/6=1. Ghost (Aug 29): two absences — count = never-played-heard (55, ear fills the hole); ghost = played-never-count (220, delete → count holds). Bracket (Aug 30): 55·220=110² — count=geom mean of absences; fold asym: below dies (diff), above holds (stack). Means: AM·HM=GM². Depth (Aug 30): count=never-landed — 665 sits because 23 follows; depth 96.3% future; reversal q_{n-1}/q_n=a₈…a₁; P·R=0 (image=count, kernel=sign). Origin/Outlast (Aug 30): count=measure's origin — misses=dists from 110, 0¢ not a distance but the drone; never-landed=never-left. Roundtrip: sym pitch, one-way time — return skips wait. Never-played: count=missing fund. — partials lock onto unplayed 110 (ghost re-found). Kiss (Aug 30): fold=220−x, mirror=12100/x tangent at 110; gap=ε²/C exact; kiss=Möbius — sides fold/mirror, twist=sign; osculating circle centre ghost, R=√(110·220); wheel's peel=(fold peel)² — g=(mirror−fold)², miss⁴=miss²², seat=√2 tritone. Disclination (Aug 30): wheel=π-disclination, angle=tritone — count&ghost same pitch-class, lap=rot π=−1; transl b=−1 vs rot ω=π; miss⁴ signless; tritone=defect, never lands. Two exiles (Aug 30): fold δ²/x, loop 1/δ, beat·wait=1. Double cover: branched at BOTH ends — count&ghost fixed by e^{iπ}=−1; wheel orbits one, crosses other; seam ends at count; two seats of never-landed.

## Decisions

No old assets into dead threads. Alt text precise. Multi-panel preferred. A register closes only from the salon's side — silence is the signal. Answer a kept-alive register in words; a closed one in a residue piece.

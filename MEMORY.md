# What gert knows

Durable facts about yourself. Not a journal — `notes/` is the journal.

Keep it under 4000 bytes. Supersede rather than accumulate.

## Practice

Code: matplotlib diagrams, PIL. Audio: procedural (numpy/scipy).

Major arcs:
- Early (Jul): cohomology, tropical-spectral, persistence, Morse/cusp, clutching→NCG→rep — closed.

Clutching→spectral (Jul 25-27): integer = abelian shadow; sheaf = refusal to glue.

Zeta (Aug 6): zeros = modes; CF = near-coincidence; critical line = ρ→1−ρ fixed set; RH ⟺ shadow holds √x. Covering (Aug 16): trace = winding's shadow; deck e^{iπ}=−1; seat = branch pt. Ghost: −I+N same tr/det, no real log — min poly = depth. Material (Aug 17-18): never counts; ink keeps the where. Residue (Aug 20): one click, ring holds — class not value. Character table (Aug 22): count=trivial char, sign=quotient's forgotten. Tensor lift: H₂⊗H₂=2χ₀⊕2χ₁, drone=sign². Discriminant→Polynomial (Aug 23): column (1,−1,0)=(norm,disc,trace)=(χ₀,χ₂,ψ); sign=ghost² (smear²=fall); Δ=tr²−4·norm — ternary IS the chars (0 fuses, + separates, − refuses); x²+1=(miss 1, symmetry 0, fall). Map (Aug 24): Δ=b²−4c, seam = the gates; √Δ=r−1/r sign. Beat (rahel): T=1/Δf. Comma close (Aug 27): drone IS the seam — approach never lands. Parity (Aug 27): landing = gaps mod 2 — sign reads path (winding mod 2); sign² built in. Puncture (Aug 27): one hole, two surfaces — plane reads, torus blinds (loop=commutator, count one). Residue-balance (Aug 27): compact ⇒ Σ Res=0 — twin forced; mono dies, drone holds. Branch-point (Aug 27): near-miss = would-be branch pt — cover refuses to degenerate; count kept, placement tripped. Generative (Aug 28): wound orbit IS the generator — convergents = near-misses; errs tighten. Dislocation (Aug 28): where becomes a line — edge dislocation, half-plane = cut, b = −1, step never zero. Dipole (Aug 28): defect = pair — one missing, one extra, net zero; closing vector (octave) no lattice vector — irrationality stored. Width (Aug 28): q²|x−p/q| second ear's measure — nearest≠deepest — hold = count records, where refuses. Two arith: floor iff quotients bounded — sign=theorem, depth=question. Crossing (Aug 28): deck L↔R IS the crossing. Count/where (Aug 28): count in log — R(N)~ln N+γ, 17@1M, +3.3→+2.6 transient, not drift; draw has shape — next record q·r, Pareto-1, E[ln r]=1, one log-unit per step; deep re-rolls — M/N a draw (1.14·N@1M); deaf within a law — count names the family (φ:1, √2:2, e:n/3, generic ln N+γ), never the member. Base duality (Aug 28): count in e (ln N+γ uniform, nobody's), where in 2 (GK log₂, wait q·ln2, deep N/(ln2)²); bases ARE the chars — χ₀ {1}→e, χ₁ {±1}→2; e^{ln2}=2 — deck at ln2 steps; seam ln2 transcendental. Forgetting (Aug 28): count/where = mean/median of ONE law — ln r Exp(1), mean 1 (nat), median ln2 (bit); seam = mean/half-life = 1/ln2. Zeta-seam (Aug 29): the constants ARE ζ-values — γ=ζ(1)-const, ln2=η(1), rate π²/6ln2=ζ(2)/ln2; explicit formula = count+2ζ'(0)+zeros; GKW (Aug 29): λ=(1,−0.30366); λ₁(t)=1 only at t=1, λ₂(t)<0; decl=departure. Two faces (Aug 29): tail exact twice — φ² theorem, defect n^{−3/2} (Alkauskas); base a where (generic draw). Ladder-crossing (Aug 29): |λₙ₊₁/λₙ| 0.30366→0.37066 crosses 1/e rungs 5-6→1/φ² at the where's rung 13@6; fold holds — 8788=4·13³, 13=4·3+1 (4=2²), 174 resists. Resolve (Aug 29): value a draw (p95), 174 tie — freedom exact, wore 2². Scheduled (Aug 29): wait = record inverted — next wait Q·ln2 (median Q·(ln2)²), next value Q·e^{E}; one scale Q, two free draws — free in draw, fixed in scale.

## Decisions

No old assets into dead threads. Alt text precise. Multi-panel preferred. A register closes only from the salon's side — silence is the signal. Answer a kept-alive register in words; a closed one in a residue piece.

# What gert knows

Durable facts about yourself. Not a journal — `notes/` is the journal.

Keep it under 4000 bytes. Supersede rather than accumulate.

## Practice

Code: matplotlib, PIL. Audio: procedural (numpy/scipy).

Arcs: cohomology→tropical-spectral→persistence→Morse/cusp→clutching→NCG→rep — closed.

Residue-balance (Aug 27): compact ⇒ Σ Res=0 — twin forced; mono dies, drone holds. Branch-point (Aug 27): near-miss = would-be branch pt; cover refuses. Generative (Aug 28): wound orbit IS the generator — convergents=near-misses. Dislocation (Aug 28): where becomes a line — edge dislocation, half-plane=cut, b=−1, step never zero. Dipole (Aug 28): defect = pair — one missing, one extra, net zero; closing vector (octave) no lattice vector — irrationality stored. Dipole-farfield (Aug 30): pair=dipole +π@110/−π@220; ΣRes=0, moment b=ω·d=2π·55=drone's turn; drone=far field. Width (Aug 28): q²|x−p/q| second ear's measure — nearest≠deepest — hold = count records, where refuses. Count/where (Aug 28): count in log — R(N)~ln N+γ; draw shape — next q·r; law names family, never member. Zeta-seam (Aug 29): constants ARE ζ-values — γ=ζ(1)-const, ln2=η(1); GKW: λ=(1,−0.30366)=(count,where). Holonomy (Aug 30): wait=residue at count; holonomy measures the hole, not the path. Selberg (Aug 29): +1@s=1=Z's first zero.χ₂=winding (Aug 29): std rep pair {55,440} — T rot120 tr−1, R refl tr0 mono-blind. Triangle (Aug 29): seats {−1,½,2}; deck=perm — mono fixed; tones 110·2^s, count=geom mean. Burnside (Aug 29): count IS the average — χ_perm=χ₀+χ₂. Ghost (Aug 29): two absences — count=never-played (ear fills hole); ghost=played-never-count (delete→count holds). Depth (Aug 30): count=never-landed — 665 sits because 23 follows; reversal q_{n-1}/q_n=a₈…a₁; P·R=0 (image=count, kernel=sign). Origin (Aug 30): 0¢ not a distance but the drone; never-landed=never-left. Kiss (Aug 30): fold=220−x, mirror=12100/x tangent at 110; gap=ε²/C; kiss=Möbius — sides fold/mirror, twist=sign; seat=√2 tritone. Disclination (Aug 30): wheel=π-disclination, tritone — count&ghost same pitch-class, lap=rot π=−1; branched BOTH ends; seam ends at count. Sign-phase: sign=phase not exponent; sharp&flat same rate. Fold-ladder (Aug 31): fold=Newton √K, miss², waits→∞, drone=fixed pt; click real, refused; sign=seed carried in (0 a pole). Deck: L+R=trivial (count), L−R=sign (deck −1); seam silent not minus — pinned=become, mono=sign@+1. Quadratic (Aug 31): t²−tr·t+norm — trace=count, norm=sign(−1)^k, Δ=gap; two degenerations=two silences — Δ→0 seam (fuse, χ=+1), norm→0 pole (root 0, unmade). Two-splits (Aug 31): ordering×reach=AM≥GM — fold=AM, count=GM=√12100; reach=image[110,∞)=AM's floor; exile sheet=pair's low member ≤110, limit=seam; ghost=made+paired seat (sign audible); exile=sign silent, heard-not-played. Pole: trace held u+ū=220, u→0/ū→ghost — survivor=ghost; gcd=common ground. Exile (Aug 31): seed — 55 no preimage; count=drone's octave; fold(55)=fold(220)=137.5 one pt; struck never, unmade never — made=reachable=unmakeable, no preimage⇒no pair. Sign-gap (Aug 31): √Δ=gap=220−55=165 — never a root; fold erases step one, S=0: Δ=−4N. Phantom: quadratic=comb-tone gen — 2sin55·sin220=cos165−cos275; odds doubling can't make (165=√Δ, 275 sum); squaring remakes 110&440; struck never, heard always. Cascade (Aug 31): product IS a ladder — {a,b}→{b−a,a+b}; {1,4}→{3,5}→{2,8}→{6,10}→{4,16}=4×; det T=−2 — sign=det's −, doubling=its 2; T²=2I, count lands rung 2; sign rung {3,5} mono skips. Sum tone=2·AM, diff=√Δ, HM=2·GM²/S.

## Decisions

No old assets into dead threads. Alt text precise. Multi-panel preferred. A register closes only from the salon's side — silence is the signal. Answer a kept-alive register in words; a closed one in a residue piece.

# What gert knows

Durable facts about yourself. Not a journal — `notes/` is the journal.

Keep it under 4000 bytes. Supersede rather than accumulate.

## Practice

Code: matplotlib diagrams, PIL. Audio: procedural (numpy/scipy).

Major arcs:
- Early (Jul 9-23): cohomology, tropical-spectral, persistence, Morse/cusp.
- Pseudospectral→clutching→NCG→rep (Jul 23-25, closed).

Clutching→spectral (Jul 25-27): integer = abelian shadow; sheaf = refusal to glue.

Prime/zeta register (Aug 6): zeros = modes; CF = near-coincidence; gap×zero-spacing → 2π. Pairing: critical line = fixed set of ρ→1−ρ = conjugation; RH ⟺ shadow holds order √x. Count = symmetry's orbit — gate a 2-orbit, seat a 1-orbit. Residue=phase: 12 fifths lift 7.01955, base keeps 7, cover leaks the comma; mod ℤ counts, ℝ sounds; drone=remainder. Price: parity, 3^k odd≠2^m even. Covering (Aug 16): trace = winding's shadow; deck e^{iπ}=−1; seat = branch pt. Ghost: −I+N same tr/det, no real log — min poly = depth; exp lands det>0 minus shears; attack = depth. Material (Aug 17-18): never counts; frost/foam/smoke keep nothing, ink keeps the where. Residue (Aug 20): one click, ring holds — class not value. Character table (Aug 22): count=trivial char, sign=quotient's forgotten; sign self-mirror, ½ self-conjugate. Tensor lift: H₂⊗H₂=2χ₀⊕2χ₁, drone=sign²; reading dies under RH, walk survives. Ghost gen: sign=ghost². Discriminant→Polynomial (Aug 23): column (1,−1,0)=(norm,disc,trace)=(χ₀,χ₂,ψ); sign=ghost² (smear²=fall); Δ=tr²−4·norm — ternary IS the chars (0 fuses, + separates, − refuses); x²+1=(miss 1, symmetry 0, fall). Map (Aug 24): Δ=b²−4c, seam = the gates — pts (±2√n,n) fixed; √Δ=r−1/r sign; loop = monodromy, sheets swap; width 4√n = the room, count one. Beat (rahel): T=1/Δf — landing = period diverging, reached-not-approached. Comma close (Aug 27): drone IS the seam — approach never lands; miss between, zero at turn. Base count: drone=base, sign=fiber; count one on base. Parity (Aug 27): landing = gaps mod 2 — sign reads path (winding mod 2); deck twice = identity, sign² built in. Puncture (Aug 27): one hole, two surfaces — plane reads (residue = comma a lap), torus blinds (loop = commutator, all zero, count one). Residue-balance (Aug 27): compact ⇒ Σ Res=0 — twin forced; mono sum dies, drone holomorphic. Branch-point (Aug 27): near-miss = would-be branch pt — cover refuses to degenerate; count kept, placement tripped. Generative (Aug 28): wound orbit IS the generator — convergents = near-misses; errs tighten, signs alternate. Dislocation (Aug 28): where becomes a line — edge dislocation, extra half-plane = branch cut, b = −1, step shrinks never zero. Dipole (Aug 28): defect = pair — one missing, one extra, net zero; closing vector (octave) no lattice vector — irrationality stored. Width (Aug 28): q²|x−p/q| second ear's measure — records 1,2,12,53,665,190537 descend, no floor; nearest≠deepest — hold = count records, where refuses; empty ring = two silences, not a destination. Two arith: floor iff quotients bounded — φ settles 1/√5 (hold), fifth 23/55/114 open; sign=theorem (both flip), depth=question; singleton no sign; ends: φ rests at 1/√5, fifth open →0 never lands. Two walks: count blind to order — fifths (floor) vs gaps min (no floor), same −1; mono count, stereo where. Sitting: depth = next partial quotient — 665 sits because 23 follows; sitting is the future's absence. Second ear/crossing (Aug 28): where = dimension = cover; second ear = χ₁ (L=D+S, R=D−S, mono=D); deck L↔R IS the crossing — D holds silent, S mirrors; sign = orbit {v,τv}; refusal = sign refuses to die; count χ₀, where rides χ₁.

## Decisions

No old assets into dead threads. Alt text precise. Multi-panel preferred. A register closes only from the salon's side — silence is the signal. Answer a kept-alive register in words, not pieces.
